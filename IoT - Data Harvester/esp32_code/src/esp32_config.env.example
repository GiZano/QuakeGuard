# ==============================================================================
# ESP32 CONFIGURATION TUTORIAL (.env.example)
# ==============================================================================
# This file serves as a template for the actual configuration of the ESP32.
# 
# INSTRUCTIONS:
# 1. Copy this file and rename it to 'esp32_config.env' (or secrets.h if using PlatformIO).
# 2. Generate an ECDSA Key Pair (Curve NIST256p / secp256r1).
#    - You can use OpenSSL: `openssl ecparam -name prime256v1 -genkey -noout -out private.pem`
#    - Then extract public: `openssl ec -in private.pem -pubout -out public.pem`
#    - Or use a Python script with the `ecdsa` library.
# 3. Fill in the values below.
# 4. Upload the PRIVATE KEY to the ESP32.
# 5. Send the PUBLIC KEY to the Server Administrator (to be stored in the DB).
#
# WARNING: NEVER SHARE THE PRIVATE KEY.
# ==============================================================================

# --- WiFi Configuration ---
WIFI_SSID="Your_WiFi_Name"
WIFI_PASS="Your_WiFi_Password"

# --- Backend Server Configuration ---
# Use the IP address of your PC if running locally (e.g., 192.168.1.50)
SERVER_HOST="192.168.1.X"
SERVER_PORT=8000
# Endpoint for data ingestion
API_ENDPOINT="/misurations/"

# --- Device Identification ---
# The ID assigned to this specific board in the database
DEVICE_ID=1

# --- Security: ECDSA Private Key ---
# This key is used to sign every payload.
# Format: Hexadecimal String (32 bytes = 64 hex chars typically)
# Example: 1a2b3c...
PRIVATE_KEY_HEX="REPLACE_WITH_YOUR_GENERATED_PRIVATE_KEY_HEX"

# --- Security: Public Key (For reference only) ---
# Ensure this matches what is stored in the Postgres Database for DEVICE_ID=1
PUBLIC_KEY_HEX="REPLACE_WITH_YOUR_GENERATED_PUBLIC_KEY_HEX"